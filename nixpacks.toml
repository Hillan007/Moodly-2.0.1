[phases.setup]
nixPkgs = ['nodejs_18', 'python311']

[phases.install]
dependsOn = ['setup']
cmds = [
    'pip install -r requirements.txt',
    'npm install',
    'npm run build'
]

[phases.build]
dependsOn = ['install']
cmds = ['echo "Build completed"']

[start]
cmd = 'python app.py'